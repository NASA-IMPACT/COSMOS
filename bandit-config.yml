# bandit-config.yml
# Only run these checks (same effective profile as the previous legacy config):
#   B105 - hardcoded password strings
#   B602 - subprocess call with shell=True
tests:
  - B105
  - B602

exclude_dirs:
  - tests
  - migrations
  - venv
